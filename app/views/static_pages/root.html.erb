<main id="root">

  <script type="text/javascript">
    window.loginImg = "<%= image_url('login_abstract.png') %>"
    window.catImg = "<%= image_url('cash-mgmt-coming.png') %>"
    window.keepTabsImg = "<%= image_url('keep-tabs.png') %>"
    window.nextLevelImg = "<%= image_url('next-level.png') %>"
    window.noManualImg = "<%= image_url('no-manual.png') %>"
    window.trustedImg = "<%= image_url('trusted.png') %>"
  </script>

  <% if logged_in? %>
    <script type="text/javascript">
      window.currentUser = <%= render(
        "api/users/user.json.jbuilder",
        user: current_user
      ).html_safe %>
    </script>
  <% end %>

</main>
